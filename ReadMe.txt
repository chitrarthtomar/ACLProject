STEPS TO PROCEED:

1) Create a database with name as "acldatabase"
2) Populate the database with the dump file provided
3) Go to frontend -> app and run npm start in the command prompt
4) Maven clean the project
5) Maven install the project
6) Run tomcat at port 8080
7) Open ACLProject\frontend\app\scripts\constants\Url.js and change the URL path to http://localhost:8080/acl.service
8) Login as admin with {username,password} as {nish,Hello}